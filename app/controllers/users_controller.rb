class UsersController < ApplicationController

  # Get the user's id and view their favorite trucks
  #def index
  #  user = User.find(current_user.id)
  #  redirect_to "/#{user.id}/favorites"
  #end
end